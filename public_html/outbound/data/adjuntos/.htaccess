# Denegar acceso directo por URL a los adjuntos (solo se sirven vía api/adjunto.php con sesión).
# Apache 2.4.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
