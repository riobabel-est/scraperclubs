==================================================
AUDITORÍA FORENSE POR BLOQUES — BLOQUE 13: AUDITORÍA DE CÓDIGO / BUGS
Fecha: 2026-08-18 03:43 (Europe/Madrid)
READ-ONLY — Sin modificaciones.
==================================================

Auditoría de bugs reales en el código (sin modificar).

--- 1. REFERENCIAS INCORRECTAS A lead_pipelines.variante ---
Búsqueda de "lead_pipelines.variante" (sin _ab): 0 resultados.
PASS. No hay referencias incorrectas.

--- 2. USO CORRECTO DE variante_ab ---
- get_cola.php: usa asignarVariante(lead_id, campaign_id) -> CORRECTO.
- enviar_lote.php: recibe variante_ab del POST, pero reservarEnvioLogico()
  lo sobreescribe con la variante determinística (INMUTABILIDAD) -> CORRECTO.
- dashboard.php: usa lp.variante_ab (lead_pipelines) para filtrar métricas
  históricas por variante. El envío real usa asignarVariante(), NO
  lead_pipelines.variante_ab. -> POSIBLE DIFERENCIA UI vs BACKEND (bug de
  presentación menor, no afecta al envío).
- js/app.js: asigna variante con Math.random() en el cliente. El backend
  (reservarEnvioLogico) la sobreescribe con la variante determinística.
  -> CÓDIGO MUERTO/INCONSISTENTE en la UI (no afecta al envío real).

--- 3. CÁLCULO DE enviados_hoy ---
CORRECTO (ver BLOQUE 9). UI (api/smtp.php get_accounts) y envío
(api/enviar_lote.php) usan comunicaciones_log con fecha de hoy como fuente
de verdad. tabs/smtp.php no recalcula (depende de la API).

--- 4. PLACEHOLDERS ---
Verificado en auditorías previas (checkpoint_auditoria_placeholders_comerciales.md).
Sin hallazgos nuevos.

--- 5. TRACKING ---
inc/metricas.php usa sqlFiltroComercial('e') correctamente (excluye TEST).
Calcula aperturas_totales y abiertos_unicos correctamente (por tracking_id).
Usa e.variant (columna real de envios, VARCHAR(1)) -> CORRECTO.

--- 6. REBOTES ---
0 registrados (ver BLOQUE 8). No hay lógica de rebotes activa en métricas.

--- 7. BAJAS ---
0 registradas (ver BLOQUE 8). Supresión gestionada por estado_lead en
esElegibleParaEnvio() (Lista Negra, Opt-Out, Unsubscribed, etc.).

--- 8. DIFERENCIAS UI vs BACKEND ---
a) dashboard.php usa lead_pipelines.variante_ab para métricas por variante,
   pero el envío usa asignarVariante(). Las métricas por variante en dashboard
   pueden no coincidir con la variante real asignada por el envío.
   -> BUG DE PRESENTACIÓN (no bloqueante, no afecta seguridad ni envío).
b) js/app.js usa Math.random() para variante en cliente. Código muerto porque
   el backend la sobreescribe. -> CÓDIGO MUERTO (no bloqueante).

--- VEREDICTO BLOQUE 13 ---
No hay bugs de seguridad ni de envío. Solo 2 hallazgos menores NO bloqueantes:
- BUG DE PRESENTACIÓN: dashboard.php usa lead_pipelines.variante_ab para
  métricas por variante (puede no coincidir con asignarVariante()).
- CÓDIGO MUERTO: js/app.js Math.random() para variante (sobreescrito por backend).
Ambos son de tipo D (bug) pero NO bloqueantes y NO afectan la integridad
ni el envío real.

--- ESTADO BLOQUE 13 ---
COMPLETADO.
