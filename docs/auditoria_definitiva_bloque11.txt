==================================================
AUDITORÍA FORENSE POR BLOQUES — BLOQUE 11: SEGURIDAD TEST/REAL
Fecha: 2026-08-18 03:34 (Europe/Madrid)
READ-ONLY — Sin modificaciones. No se ejecutaron envíos.
Inspección de código existente.
==================================================

Auditoría de aislamiento TEST/REAL en el código.

--- inc/eligibilidad.php ---
- esLeadTest(array $lead): detecta lead TEST por email @futprotec.local
  (case-insensitive) o nombre_club test*.
- esCampanaTest(SQLite3 $db, int $idCampana): detecta campaña TEST.
- esEnvioTest(array $envio): detecta envío TEST por envios.es_test=1
  (fuente de verdad primaria) + red de seguridad legacy (email @futprotec.local
  o club test*).
- sqlFiltroComercial(): filtro SQL para excluir TEST de métricas comerciales.
- sqlFiltroCompatibilidadLeadCampana(): si campaña es TEST -> solo leads TEST;
  si campaña es REAL -> excluye leads TEST
  (NOT (LOWER(email) LIKE '%@futprotec.local%' OR LOWER(nombre_club) LIKE 'test%')).
- esElegibleParaEnvio(): comprueba compatibilidad campaña/lead ANTES del envío.
- reservarEnvioLogico(): INMUTABILIDAD — para campaña real la variante es
  determinística e independiente del valor del llamador (impide random por envío).

--- api/get_cola.php ---
- Usa sqlFiltroCompatibilidadLeadCampana($db, $idCampana) para filtrar leads.
- Usa asignarVariante((int)$lead['id'], $idCampana) para la variante
  (NO usa lead_pipelines.variante_ab).

--- api/enviar_lote.php ---
- Usa esElegibleParaEnvio() (visto en BLOQUE 5/9) antes del envío.
- Usa la métrica real de comunicaciones_log para el límite diario.

--- cli/cron.php ---
- Usa sqlFiltroCompatibilidadLeadCampana($db, $campaignId).
- Usa esElegibleParaEnvio($db, leadId, campaignId) y bloquea si !$elig['ok'].
- Usa variante determinística (FASE 3).

--- CONFIRMACIONES DE AISLAMIENTO ---
TEST → REAL = BLOQUEADO:
  sqlFiltroCompatibilidadLeadCampana excluye leads TEST de campañas reales.
REAL → TEST = BLOQUEADO:
  campaña TEST solo acepta leads TEST (filtro inverso).
TEST en producción = BLOQUEADO:
  esEnvioTest marca es_test=1; sqlFiltroComercial excluye TEST de métricas.
A/B/C histórico NO controla envío:
  reservarEnvioLogico recalcula la variante determinística (asignarVariante),
  NO usa lead_pipelines.variante_ab como fuente de verdad del envío.

--- VEREDICTO BLOQUE 11 ---
PASS. Aislamiento TEST/REAL correctamente implementado en todos los puntos
de envío (get_cola, enviar_lote, cron). Sin riesgo de envío accidental
TEST→REAL ni REAL→TEST.

--- ESTADO BLOQUE 11 ---
COMPLETADO.
